package serverModules.processors;

public class ObjectProcessor implements ProcessAble<Object> {

    @Override
    public Object process(Object obj) {
        // Perform some processing on the object
        return obj;
    }
}